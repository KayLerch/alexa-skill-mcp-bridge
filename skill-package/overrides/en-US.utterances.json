{
  "_comment": "Developer additions merged into the generated model on every `npm run generate`. Keys are intent names; values are sample utterances following Alexa's rules (lowercase words, known {slots}, one AMAZON.SearchQuery slot alone with a carrier phrase).",
  "SearchHotelsIntent": ["find me a place to stay in {destination}", "hotels in {destination}"],
  "GetWeatherIntent": ["weather in {city}", "how is the weather in {city}"]
}
